# Caddy reverse proxy for PowVal. {$DOMAIN} is substituted by setup-vps.sh.
# HTTPS certificates are obtained and renewed automatically (Let's Encrypt).

{$DOMAIN} {
	encode zstd gzip
	reverse_proxy 127.0.0.1:3000
}

www.{$DOMAIN} {
	redir https://{$DOMAIN}{uri} permanent
}
